import { assertEquals } from "@std/assert";
import {  } from "./puzzle05.ts";

Deno.test("countXMAS - test case 1", () => {
  const result = 0
  const expected = 0;
  assertEquals(
    result,
    expected,
    `Expected ${18} but got ${result} for input test puzzle input.`,
  );
});
